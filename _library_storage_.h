#ifndef __LIBRARY_STORAGE_H_
#define __LIBRARY_STORAGE_H_

// Including some libraries to manipulate/control everything about Input/Output Formatting
#include <ios>
#include <iomanip>
#include <iostream>

// Including some libraries to manipulate/control everything about String or an array of characters or single character
#include <string>
#include <cctype>
#include <cstring>

// Including library to manipulate/control everything about Window,....
#include <dos.h>
#include <conio.h>
#include <Windows.h>

// Including some libraries to manipulate/control everything about File, Folder, ...
#include <fstream>
#include <sys/stat.h>

// Using this namespace as a global namespace and make it hidden behind for every types, functions,... belong to the standard library
using namespace std;

const string defaultPassword = "123456";

#endif // !__LIBRARY_STORAGE_H_
